[
  {
    "name": "share channel callback shows share prompt",
    "steps": [
      {
        "send": { "callback": "share:channel" },
        "expect": [{ "method": "editMessageText" }]
      }
    ]
  },
  {
    "name": "share title callback shows share message",
    "steps": [
      {
        "send": { "callback": "share:title:le-roi-lion" },
        "expect": [{ "method": "editMessageText" }]
      }
    ]
  }
]
