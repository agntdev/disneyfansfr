[
  {
    "name": "receive private callback shows prompt",
    "steps": [
      {
        "send": { "callback": "receive:private" },
        "expect": [{ "method": "editMessageText" }]
      }
    ]
  },
  {
    "name": "receive title callback sends private message",
    "steps": [
      {
        "send": { "callback": "receive:title:le-roi-lion" },
        "expect": [{ "method": "editMessageText" }]
      }
    ]
  }
]
