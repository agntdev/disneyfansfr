[
  {
    "name": "subscribe updates callback shows prompt",
    "steps": [
      {
        "send": { "callback": "subscribe:updates" },
        "expect": [{ "method": "editMessageText" }]
      }
    ]
  },
  {
    "name": "subscribe title callback confirms subscription",
    "steps": [
      {
        "send": { "callback": "subscribe:title:le-roi-lion" },
        "expect": [{ "method": "editMessageText" }]
      }
    ]
  }
]
